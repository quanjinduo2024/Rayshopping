import { Card, Typography } from 'antd'

const { Title } = Typography

const Checkout = () => {
  return (
    <div>
      <Title level={2}>结算</Title>
      <Card>
        <p>结算页面（开发中）</p>
      </Card>
    </div>
  )
}

export default Checkout
